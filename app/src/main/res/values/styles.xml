<resources>

    <style name="Base.Theme.AppCompat.Light.DarkActionBar" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorButtonNormal">@color/colorButtonNormal</item>
    </style>

    <style name="style_score">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:text">@string/text_score</item>
        <item name="android:textColor">@color/colorTextScore</item>
        <item name="android:textSize">@dimen/text_score</item>
    </style>

    <style name="style_button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">@dimen/text_button</item>
        <item name="android:textColor">@color/colorTextButton</item>
    </style>

    <style name="style_team">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorTextTeam</item>
        <item name="android:textSize">@dimen/text_team</item>
    </style>

</resources>